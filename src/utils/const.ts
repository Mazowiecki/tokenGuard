import dictionary from '@/dictionary/en.json';

export const baseUrl = 'https://api.tokenguard.io';

export const periodOptions = {
  lastMonth: dictionary.lastMonth,
  lastTwoWeeks: dictionary.last2Weeks,
  lastWeek: dictionary.lastWeek,
};
